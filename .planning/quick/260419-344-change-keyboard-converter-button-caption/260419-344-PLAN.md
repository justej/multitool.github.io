---
phase: quick
plan: 260419-344
type: execute
wave: 1
depends_on: []
files_modified:
  - registry.js
  - index.html
autonomous: true
requirements: [QUICK-344]

must_haves:
  truths:
    - "Left-to-right button reads '→ ЙЦУКЕН'"
    - "Right-to-left button reads 'QWERTY ←'"
  artifacts:
    - path: registry.js
      provides: "Updated action labels for keyboard tool"
      contains: "→ ЙЦУКЕН"
    - path: index.html
      provides: "Updated initial button text"
      contains: "→ ЙЦУКЕН"
  key_links:
    - from: registry.js
      to: index.html
      via: "Both must match — registry drives dynamic renders, index.html is the static initial state"
---

<objective>
Shorten keyboard converter button captions:
- "QWERTY → ЙЦУКЕН" → "→ ЙЦУКЕН"
- "ЙЦУКЕН → QWERTY" → "QWERTY ←"

Purpose: Reduce button label width for a tighter UI.
Output: Updated labels in registry.js and index.html.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@registry.js
@index.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update button labels in registry.js and index.html</name>
  <files>registry.js, index.html</files>
  <action>
    In registry.js, update the `actions` array for the `keyboard` tool (lines 25-26):
    - Change `'QWERTY → ЙЦУКЕН'` → `'→ ЙЦУКЕН'`
    - Change `'ЙЦУКЕН → QWERTY'` → `'QWERTY ←'`

    In index.html, update the two action buttons (lines 27-28):
    - Change `QWERTY → ЙЦУКЕН` → `→ ЙЦУКЕН`
    - Change `ЙЦУКЕН → QWERTY` → `QWERTY ←`
  </action>
  <verify>
    Open index.html in a browser (or inspect the files): both action buttons show the new short captions. Also verify with:
    grep "→ ЙЦУКЕН\|QWERTY ←" registry.js index.html
  </verify>
  <done>Both files contain the new short labels; old labels "QWERTY → ЙЦУКЕН" and "ЙЦУКЕН → QWERTY" are gone.</done>
</task>

</tasks>

<verification>
grep -c "QWERTY → ЙЦУКЕН" registry.js index.html   # must return 0 for both
grep "→ ЙЦУКЕН" registry.js index.html              # must match in both files
grep "QWERTY ←" registry.js index.html              # must match in both files
</verification>

<success_criteria>
Button labels in both registry.js and index.html are updated to the shorter form. No other content changed.
</success_criteria>

<output>
No SUMMARY required for quick tasks.
</output>
